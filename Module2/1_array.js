// arrays are ordered collection of data
// it can store different data types

var arr = [12, "Lamborghini", true, 3.7];
console.log(arr);

// initializing arr
console.log(arr[0]);

// updating the array
arr[2] = false;
console.log(arr);

// length of an array
console.log(arr.length);
